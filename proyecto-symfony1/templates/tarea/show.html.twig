{% extends 'base.html.twig' %}

{% block title %}Tarea{% endblock %}

{% block body %}
    <h1>Tarea</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Título</th>
                <td>{{ tarea.titulo }}</td>
            </tr>
            <tr>
                <th>Contenido</th>
                <td>{{ tarea.contenido }}</td>
            </tr>
            <tr>
                <th>Prioridad</th>
                <td>{{ tarea.prioridad }}</td>
            </tr>
            <tr>
                <th>Horas</th>
                <td>{{ tarea.horas }}</td>
            </tr>
            <tr>
                <th>Creado</th>
                <td>{{ tarea.createdAt ? tarea.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Actualizado</th>
                <td>{{ tarea.updatedAt ? tarea.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ tarea.id }}</td>
            </tr>
        </tbody>
    </table>

    <div class="p-2 g-col-6">
    <a class="btn btn-info" href="{{ path('app_tarea_edit', {'id': tarea.id}) }}" role="button">Editar</a> 
    </div>

    {{ include('tarea/_delete_form.html.twig') }}
    
    <div class="p-2 g-col-6">
    <a class="btn btn-success" href="{{ path('app_tarea_index') }}" role="button">Volver a la lista</a>
    </div>
{% endblock %}
