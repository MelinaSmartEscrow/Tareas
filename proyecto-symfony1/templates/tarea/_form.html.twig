{{ form_start(form) }}

    <h6 class="modal-title">Título</h6>
    {{ form_widget(form.titulo, { 'attr': {'class': 'form-control'} }) }}

    <h6 class="modal-title">Contenido</h6>
    {{ form_widget(form.contenido, { 'attr': {'class': 'form-control'} }) }}

    <h6 class="modal-title">Prioridad</h6>
    {{ form_widget(form.prioridad, { 'attr': {'class': 'form-control'} }) }}

    <h6 class="modal-title">Horas</h6>
    {{ form_widget(form.horas, { 'attr': {'class': 'form-control'} }) }}

    
    <div class="p-2 g-col-6">
    <button class="btn btn-primary">{{ button_label|default('Guardar') }}</button>
    </div>
{{ form_end(form) }}

